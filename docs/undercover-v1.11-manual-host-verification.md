# A2 V1.11 验证与交接

日期：2026-09-07。范围见 [SPEC](./undercover-v1.11-manual-host-spec.md)。

## 自动验证

- 手动模式房主标记为出题人，不计入容量、发牌、可操作成员、投票和胜负。
- 系统随机模式房主仍作为第 1 名玩家。
- V3.4 SQL 在本地 PostgreSQL 运行时验证了“1 名出题人＋3 名玩家”、房间缩容和满员拒绝。
- `npm test`：132/132 通过，其中包含 2 项 A2 V1.11 SQL 运行时测试。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过；`/undercover` 与其余静态路由均成功生成。
- V3.4 对重复操作编号增加参与者绑定校验，其他玩家复用同一编号时明确拒绝。
- CloudBase 迁移由用户执行，V3.4 两项 RPC、匿名执行权限、手动房主容量、等待房间人数统计与 V3.1 委托共 6/6 项只读核验通过。

## 手动操作

1. CloudBase V3.4 迁移及核验已经完成。
2. 部署本次 `test` 提交的前端。
3. 用 4 个独立浏览器会话验证手动 3 人局：1 名出题房主＋3 名玩家；另用 3 个会话回归系统随机 3 人局。

云端 SQL 由用户执行；本次代码将推送 `test`，未部署、未合并 `main`。
