# V1.10.2 验证与交接

日期：2026-09-07。范围见 [界面 SPEC](./ui-v1.10.2-spec.md)。

## 已完成

- `npm test`：127/127 通过（原有 120 项 + 7 项工作簿 UI 检查）。
- `npm run lint`：通过，无警告。
- `npx tsc --noEmit` 及生产构建类型检查：通过。
- `npm run build`：完整静态导出目录、A2、A3、A4、A5。
- 五个本地路径均 HTTP 200，均包含新工作簿组件与 V1.10.2。
- 测试直接渲染真实 React 组件，检查五页中性标题、列结构、非浮动状态、批注转义与可访问性、工具栏事件、帮助返回与私密内容的批注边界。
- 原有游戏规则、隐私和本地数据库回归测试通过。没有连接云端执行 SQL。

## 尚未完成，不能当作已通过

内置浏览器多次报告无法连接 webview，连现有预览页的 DOM 也无法读取。因此本轮没有拿到截图，未实测以下视口或真实多人点击流程：

| 尺寸 | 重点 | 状态 |
|---|---|---|
| 1366×768 | 主操作不被批注遮挡；全文可读，Sheet 可点击 | 待浏览器实测 |
| 1920×1080 | 列宽合理，通知侧栏与表格共存 | 待浏览器实测 |
| 约 1024px 宽 | 通知上下排列，不把正文挤出窗口 | 待浏览器实测 |
| 手机基本操作 | 原有简化输入入口与横向表格滚动保留 | 待浏览器实测 |

代码与构建验证不替代上述视觉及多人验收。

## 建议预览顺序

1. 目录：点击摘要 → 底部批注 → 收起／展开／关闭；切到帮助再返回。
2. A2：创建配置与规则说明；本机预览中查看词牌，确认公式栏无词语；失焦、Esc、切工作表后遮挡。轮次记录在统一公开前不可见其他人的描述。
3. A3：普通＋角色扮演默认值；本题说明、评分参考、公开提示批注；顶部开始／记录／统计和帮助返回。
4. A4：两段长陈词的多行编辑与确认锁定；公开陈词批注、两项选票、工作表切换。
5. A5：等待时草稿与正式提交的区别；汤主资料不出现在批注；公开问答批注与私密遮挡。
6. 各尺寸打开通知并展开批注，确认仍能操作和滚动表格；确认普通消息在状态栏，失败标注“需要处理”而不弹窗。

## 交付边界

- 用户已于 2026-09-07 明确确认推送 `test`；本次版本基于 `6405e75`，以承载本文档的 Git 提交作为版本记录。
- 没有合并 `main`、部署站点或执行云端 SQL；无需新增 SQL。
- 本地临时预览为 `http://127.0.0.1:59219/`，仅在本次本机进程存活期间可用。
- 预览进程关闭后，可运行 `node node_modules/next/dist/bin/next dev --hostname 127.0.0.1 --port 0`，访问终端给出的新地址。
- 本次仅向 GitHub `test` 交付源码；用户随后重构托管站并进行视觉及多人验收，稳定版合并仍单独确认。
